{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "npm run lint 1>&2; LINT_EXIT=$?; if [ $LINT_EXIT -ne 0 ]; then printf '{\"continue\":false,\"stopReason\":\"Lint fallo — hay errores de codigo. Corrigelos antes de hacer git push.\"}'; fi",
            "statusMessage": "Verificando lint antes del push...",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
